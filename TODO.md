# Deploy to Vercel (free demo for friends)
See [DEPLOY.md](./DEPLOY.md). **Hobby only, $0 API:** commit `data/lyrics.json` + `results.json`, `npm run demo:replay` for full-catalog lyrics + scan replay in the browser (`NEXT_PUBLIC_DEMO_MODE`), block live POST routes in prod. Real scans stay local.

# Refactor backend to python - useful for working on agent orchestration

# Visual timeline
How has shoutout volume changed over time? Add a chart so we can see, per project, who the top n referenced Friends were

# Make a new song
We have an epic catalog of who Drizzy references and how, as well as all of his lyrics. Given a user's seleciton of Friends and an optional topic, generate a realistic and creative new Drizzy verse that shouts out the selected Friends (possibly in a way consistent with previous references) in a topical and Drake-like way

